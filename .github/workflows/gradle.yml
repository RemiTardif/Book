name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build
        run: |
          chmod +x gradlew
          ./gradlew build

      - name: Run tests
        run: |
          chmod +x gradlew
          ./gradlew test

      - name: Publish test results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: build/test-results/**/*.xml

      - name: JaCoCo coverage report
        run: |
          chmod +x gradlew
          ./gradlew jacocoTestReport

      - name: Publish JaCoCo report
        uses: Madrapps/jacoco-report@v1.6.1
        with:
          paths: build/reports/jacoco/test/jacocoTestReport.xml
          token: ${{ secrets.GITHUB_TOKEN }}
          min-coverage-overall: 0

      - name: Run mutation tests
        run: |
            chmod +x gradlew
            ./gradlew pitest

      - name: Run integration tests
        run: |
            chmod +x gradlew
            ./gradlew testIntegration

      - name: Run component tests
        run: |
            chmod +x gradlew
            ./gradlew testComponent

      - name: Run Detekt
        run: |
            curl -sSLO https://github.com/detekt/detekt/releases/download/v1.23.6/detekt-cli-1.23.6-all.jar
            java -jar detekt-cli-1.23.6-all.jar --input src --config config/detekt.yml --report txt:build/reports/detekt.txt || true